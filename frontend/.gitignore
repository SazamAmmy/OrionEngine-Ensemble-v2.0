#===============================================================================
# Flutter/Dart specific
#===============================================================================

# General Dart/Pub generated files
.dart_tool/
.packages
# package_config.json is generated by `flutter pub get` and contains paths specific
# to the user's machine. It SHOULD be regenerated via `flutter pub get`.
# The pubspec.lock file MUST be committed to ensure reproducible builds.
package_config.json
# package_config_subset is also generated and should not be committed.
package_config_subset

# Build directory
build/

# Flutter tool files
.flutter-plugins-dependencies # Generated file, should be regenerated

# VS Code files (optional, but common)
.vscode/

# IntelliJ/Android Studio files
.idea/
*.iml
*.ipr
*.iws
# Your specific iml file (already covered by *.iml)
# sustainableapp.iml

# Log files
*.log

# Crash reports
# These files are generated by the Flutter tooling in the event of a crash.
# They should not be version controlled. They are located in the root of the
# project.
app*.dmp*
app*.jenc*
hs_err_pid*.log
replay_pid*.log

# Directory created by some tooling or extensions (as seen in your list)
extension_discovery/
flutter_build/

#===============================================================================
# Platform specific build files (even if you don't use all platforms now)
#===============================================================================

# Android specific
android/app/build/
android/build/
android/.gradle/
android/gradle/wrapper/gradle-wrapper.jar
android/gradlew
android/gradlew.bat
android/local.properties # Contains SDK path, specific to each developer machine
android/key.properties   # Often contains signing key passwords, DO NOT COMMIT

# iOS specific
ios/build/
ios/.symlinks/
ios/Pods/                  # CocoaPods generated files
ios/Flutter/App.framework
ios/Flutter/Flutter.podspec
ios/Flutter/Generated.xcconfig
ios/Flutter/ephemeral/ # Contains generated Xcode settings, script phases, etc.
ios/Flutter/flutter_export_environment.sh
ios/Podfile.lock         # Similar to pubspec.lock, but for iOS native dependencies.
                         # Often committed, but can be ignored if Podfile specifies exact versions.
                         # Flutter default template ignores it.
ios/.generated/
ios/Runner/GeneratedPluginRegistrant.* # Generated files
ios/Runner.xcworkspace/xcuserdata/ # User specific Xcode settings
ios/Runner.xcodeproj/project.xcworkspace/ # Xcode workspace settings
ios/Runner.xcodeproj/xcuserdata/ # User specific Xcode settings

# Linux specific
linux/build/
linux/flutter/ephemeral/

# macOS specific
macos/build/
macos/Flutter/ephemeral/
macos/.DS_Store # macOS specific file system metadata

# Windows specific
windows/build/
windows/flutter/ephemeral/
windows/runner/GeneratedPluginRegistrant.rc # Generated file

# Web specific
web/build/
web/.dart_tool/

#===============================================================================
# Other common ignores
#===============================================================================
# Ignore sensitive information or local configuration files if you have any
# secrets.yaml
# .env

# Optional: Ignore the 'version' file if it's automatically generated
# version